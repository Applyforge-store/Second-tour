import Link from "next/link";
import { leadAction } from "./actions";

export default async function DemoPage({searchParams}:{searchParams:Promise<{sent?:string;error?:string}>}){const q=await searchParams;return <main className="auth-shell"><section className="auth-card"><Link className="brand" href="/"><span className="mark">2</span>Second Tour</Link><h1>Demander une démonstration</h1><p className="muted">L’offre fondatrice est proposée à 39 €/mois, sans engagement, après validation du besoin et du cadre de traitement des données.</p>{q.sent&&<p className="success">Demande reçue. Nous vous répondrons par e-mail.</p>}{q.error&&<p className="error">{q.error}</p>}{!q.sent&&<form action={leadAction}><div style={{position:"absolute",left:"-9999px"}} aria-hidden="true"><label>Site web<input name="website" tabIndex={-1} autoComplete="off"/></label></div><div className="field"><label htmlFor="garageName">Nom du garage</label><input id="garageName" name="garageName" required/></div><div className="field"><label htmlFor="contactName">Votre nom</label><input id="contactName" name="contactName" required/></div><div className="field"><label htmlFor="email">E-mail professionnel</label><input id="email" name="email" type="email" required/></div><div className="field"><label htmlFor="message">Votre besoin (facultatif)</label><textarea id="message" name="message"/></div><button className="button" type="submit">Envoyer la demande</button></form>}</section></main>}
